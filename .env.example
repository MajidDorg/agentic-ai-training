# Copy this file to ".env" (exactly that, 4 characters) and paste your real key after the = sign.
# Get a key at https://platform.openai.com/api-keys  (it starts with sk-proj-)
# The .env file is gitignored, so your key never gets committed or pushed.

OPENAI_API_KEY=
